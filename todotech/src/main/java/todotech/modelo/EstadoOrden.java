package todotech.modelo;

/**
 * Estados posibles de una OrdenVenta.
 * RF-04 (CREADA), RF-16 (PAGADA), RF-20 (CERRADA), RF-24 (CANCELADA), MOD-09 (DEVUELTA)
 */
public enum EstadoOrden {
    CREADA,
    PAGADA,
    CERRADA,
    CANCELADA,
    DEVUELTA
}
